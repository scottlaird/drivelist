# Cut a release by pushing a tag: git tag v0.1.0 && git push origin v0.1.0
# Builds the .debs for amd64, arm64 and armhf with nfpm and attaches them,
# plus the bare binaries, to a GitHub release for the tag.
name: Release

on:
  push:
    tags: [ "v*" ]

permissions:
  contents: write

jobs:
  release:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 0   # the Makefile derives the version from the tag
      - uses: actions/setup-go@v5
        with:
          go-version-file: go.mod
      - name: test
        run: go test ./...
      - name: install nfpm
        run: go install github.com/goreleaser/nfpm/v2/cmd/nfpm@latest
      - name: package
        run: make deb VERSION="${GITHUB_REF_NAME#v}"
      - name: release
        env:
          GH_TOKEN: ${{ github.token }}
        # The release may already exist: creating one in the GitHub UI is
        # what pushes the tag that triggers this workflow. Then only the
        # assets are missing.
        run: |
          if gh release view "$GITHUB_REF_NAME" >/dev/null 2>&1; then
            gh release upload "$GITHUB_REF_NAME" --clobber dist/*.deb dist/drivelist-linux-*
          else
            gh release create "$GITHUB_REF_NAME" --generate-notes --title "$GITHUB_REF_NAME" \
              dist/*.deb dist/drivelist-linux-*
          fi
